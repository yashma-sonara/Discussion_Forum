.comment 
    %p= comment.comment
    %p= comment.user.email
    
    = link_to "Edit", edit_post_comment_path(comment.post, comment)
    = link_to "Delete Reply", delete_comment_path(comment), method: :destroy, data: { confirm: "Are you sure?" }
